# Server Configuration
# Copy this file to 'server' and fill in your actual values

# Backend server configuration
# Required: Backend server port (default: 8000)
BACKEND_PORT=8000

# Required: Backend server host (default: 0.0.0.0 for all interfaces)
BACKEND_HOST=0.0.0.0

# Frontend server configuration
# Required: Frontend server port (default: 3000)
FRONTEND_PORT=3000

# Required: Frontend URL for CORS and OAuth redirects
# In production, this would be your actual domain (e.g., https://gravly.com)
FRONTEND_URL=http://localhost:3000

# Required: Backend URL for frontend API calls
# In production, this might be different (e.g., https://api.gravly.com)
BACKEND_URL=http://localhost:8000
